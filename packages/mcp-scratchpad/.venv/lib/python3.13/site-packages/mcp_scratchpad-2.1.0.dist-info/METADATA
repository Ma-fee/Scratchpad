Metadata-Version: 2.3
Name: mcp-scratchpad
Version: 2.1.0
Summary: 基于 FastMCP 的高质量文件管理服务器
Author: Leoyzen
Author-email: Leoyzen <leoyzen@gmail.com>
Requires-Dist: fastmcp>=2.13.1
Requires-Dist: fsspec>=2024.1.0
Requires-Dist: httpx<1.0
Requires-Dist: httpx-sse>=0.4.3
Requires-Dist: pillow>=10.0.0
Requires-Dist: pydantic>=2.12.4
Requires-Dist: pydantic-settings>=2.0.0
Requires-Dist: pyyaml>=6.0.0
Requires-Dist: pypdf>=3.0.0
Requires-Dist: black>=23.0.0 ; extra == 'dev'
Requires-Dist: ruff>=0.1.0 ; extra == 'dev'
Requires-Dist: mypy>=1.8.0 ; extra == 'dev'
Requires-Dist: pre-commit>=3.6.0 ; extra == 'dev'
Requires-Dist: moto[s3]>=5.0.0 ; extra == 'moto'
Requires-Dist: boto3>=1.34.0 ; extra == 'moto'
Requires-Dist: s3fs>=2024.1.0 ; extra == 's3'
Requires-Dist: pytest>=8.0.0 ; extra == 'test'
Requires-Dist: pytest-asyncio>=0.23.0 ; extra == 'test'
Requires-Dist: pytest-benchmark>=4.0.0 ; extra == 'test'
Requires-Dist: pytest-cov>=4.0.0 ; extra == 'test'
Requires-Dist: pytest-mock>=3.12.0 ; extra == 'test'
Requires-Dist: factory-boy>=3.3.0 ; extra == 'test'
Requires-Dist: faker>=20.0.0 ; extra == 'test'
Requires-Python: >=3.10
Provides-Extra: dev
Provides-Extra: moto
Provides-Extra: s3
Provides-Extra: test
Description-Content-Type: text/markdown

# MCP Scratchpad Service

基于 FastMCP 的高质量 Scratchpad 文件管理服务，提供 MCP Server（stdio / SSE），方便各类 Agent 或 IDE 使用。所有真实文件都会落到 `out/files/{session_id}/`，便于调试和回溯。

## 🚀 新特性 (v2.1.0)

- **🔧 配置管理**: 环境变量驱动的配置系统，支持灵活部署
- **🛡️ 增强错误处理**: 精细化异常分类和统一的错误响应
- **🏥 健康检查**: 内置系统健康监控和状态检查工具
- **🧪 现代化测试**: 基于 pytest 的完整测试套件
- **📝 结构化日志**: 支持文本和 JSON 格式的日志输出

## 环境准备

```bash
cd packages/mcp-scratchpad
uv sync

# 安装开发依赖（可选）
uv sync --group test --group dev
```

## FastMCP 服务器

### stdio（默认）

```bash
uv run mcp-scratchpad
# 或
uv run python -m mcp_scratchpad.server
```

### SSE HTTP

```bash
uv run mcp-scratchpad --transport sse --host 0.0.0.0 --port 8890
```

### 使用 FastMCP 配置文件

```bash
fastmcp run fastmcp.json
```

### 脚本入口

项目已配置脚本入口，可以直接使用：

```bash
# 主服务器（支持命令行参数）
mcp-scratchpad --transport stdio
mcp-scratchpad --transport sse --host 0.0.0.0 --port 8890
```

## 目录说明

- `server.py`：FastMCP 服务器主入口（集成配置管理）
- `config/`：配置管理模块
  - `settings.py`：配置类定义和环境变量支持
  - `validation.py`：配置验证逻辑
- `tools/`：FastMCP 工具实现
  - `file_tools.py`：文件操作工具（增强错误处理）
  - `health_tools.py`：健康检查工具
- `exceptions/`：异常处理模块
  - `custom.py`：自定义异常类
  - `handlers.py`：异常处理器
- `monitoring/`：监控模块
  - `health.py`：健康检查实现
- `models.py`：Pydantic 请求模型与 `FileRecord`
- `storage.py`：文件落盘与 metadata 管理
- `utils.py`：路径校验、错误包装等工具
- `tests/`：pytest 测试套件
  - `unit/`：单元测试
  - `conftest.py`：测试配置和夹具
- `docs/`：详细文档

## FastMCP 工具

### list_files

列出会话文件，支持多种过滤条件。

- `session_id`: 会话ID
- `persistent`: 过滤持久化文件
- `keyword`: 文件名关键词
- `namespace`: 命名空间过滤
- `agent_name`: Agent名称过滤

### read_file

读取文件内容，支持行范围。

- `session_id`: 会话ID
- `file_path`: 单个文件路径
- `file_paths`: 多个文件路径列表
- `file_requests`: 详细读取请求（包含行范围）

### write_file

写入文件内容。

- `session_id`: 会话ID
- `mode`: create/update
- `file_name`: 创建模式下的文件名
- `agent_name`: Agent名称
- `file_path`: 更新模式下的文件路径
- `content`: 文件内容
- `overwrite`: 是否覆盖（**默认为 true**，以提升用户体验）
- `persistent`: 是否持久化
- `permission`: 文件权限
- `expected_version`: 乐观锁版本

> **重要变更**：从 v2.1.0 开始，`overwrite` 参数默认值已改为 `true`，以减少 `FILE_ALREADY_EXISTS` 错误。如需保护文件不被覆盖，请显式设置 `overwrite=false`。

### remove_file

删除文件。

- `session_id`: 会话ID
- `file_path`: 文件路径
- `expected_version`: 乐观锁版本
- `force`: 强制删除持久化文件

### health_check

执行系统健康检查，返回各组件状态。

- 返回存储、配置和系统整体状态
- 包含错误详情和组件健康信息

### system_info

获取系统信息和服务器状态。

- 返回启动时间、运行时长、版本信息
- 用于监控和调试

### storage_status

检查存储系统状态。

- 验证存储目录可访问性
- 检查磁盘空间和写入权限

## 使用示例

### 基本文件操作

```python
# 通过 MCP 客户端调用工具

# 列出文件
result = await client.call_tool("list_files", {
    "session_id": "user_123-agent_456",
    "agent_name": "assistant",
    "keyword": "config"
})

# 写入文件
result = await client.call_tool("write_file", {
    "session_id": "user_123-agent_456",
    "mode": "create",
    "agent_name": "assistant",
    "file_name": "config.json",
    "content": '{"theme": "dark", "version": "1.0"}',
    "permission": "read_write"
})

# 读取文件
result = await client.call_tool("read_file", {
    "session_id": "user_123-agent_456",
    "file_path": "session/assistant/config.json"
})

# 删除文件
result = await client.call_tool("remove_file", {
    "session_id": "user_123-agent_456",
    "file_path": "session/assistant/config.json"
})
```

## 注意事项

- 所有写入路径必须位于 `session/{agent_name}/` 命名空间；读取可以访问 `session/` 与 `kb/`。
- `expected_version` 用于乐观锁，缺失视作强制覆盖，可通过 `list_files` / `read_file` 的 `version` 字段获取最新值。
- `force=true` 才能删除 `persistent=true` 的文件。
- 默认单文件大小限制 10MB，可在 `storage.FileSystemStore(size_limit_bytes=...)` 调整。
- 若通过 SSE 对外暴露，请自行在代理层增加认证、限流等安全策略。
- **文件覆盖行为**：`write_file` 工具默认允许覆盖已存在文件（`overwrite=true`），如需保护文件请显式设置 `overwrite=false`。

## 迁移说明

本项目已从官方 MCP 库迁移到 FastMCP 框架，主要改进：

### 技术优势

- **代码简化**: 装饰器模式大幅减少样板代码
- **功能增强**: Context 对象提供进度报告和日志记录
- **类型安全**: 更好的参数验证和错误处理
- **现代化**: 基于 FastMCP 的最新特性

### 兼容性

- 保持所有 MCP 协议接口不变
- 保持现有的文件存储格式
- 支持原有的 stdio 和 SSE 传输方式

### 新特性

- 使用 `uv run mcp-scratchpad` 启动服务器
- 支持 FastMCP 配置文件
- 更好的错误处理和用户反馈
- Context 对象提供进度报告
- 使用标准 Python logging 而不是 print
- 脚本入口支持直接命令行调用

### 架构简化

- 移除了 FastAPI 依赖，专注于 MCP 协议
- 更清晰的项目结构
- 更好的日志管理
- 脚本入口配置

详细迁移文档请参考 `docs/` 目录下的相关文档。

## 开发和测试

### 运行测试

```bash
# 运行 pytest 测试套件
uv run pytest

# 运行测试并生成覆盖率报告
uv run pytest --cov=src --cov-report=html

# 运行特定测试
uv run pytest tests/unit/test_storage.py

# 运行带标记的测试
uv run pytest -m unit  # 单元测试
uv run pytest -m integration  # 集成测试

# 运行旧版基本功能测试（向后兼容）
uv run python test_basic_functionality.py
```

### 代码质量检查

```bash
# 代码格式化
uv run black src/ tests/

# 代码检查
uv run ruff check src/ tests/

# 类型检查
uv run mypy src/
```

### 日志配置

项目使用 Python 标准 logging 模块，支持文本和 JSON 格式：

```bash
# 设置日志级别
export MCP_SCRATCHPAD_LOG_LEVEL=DEBUG

# 设置日志格式
export MCP_SCRATCHPAD_LOG_FORMAT=json  # 或 text

uv run mcp-scratchpad
```

## 配置选项

### 命令行参数

- `--transport`: 传输方式（stdio/sse）
- `--host`: SSE 主机地址（默认：0.0.0.0）
- `--port`: SSE 端口（默认：8890）
- `--base-dir`: 文件存储基础目录（默认：out/files）

### 环境变量

所有配置项都支持环境变量，使用 `MCP_SCRATCHPAD_` 前缀：

- `MCP_SCRATCHPAD_HOST`: 服务器主机地址
- `MCP_SCRATCHPAD_PORT`: 服务器端口
- `MCP_SCRATCHPAD_TRANSPORT`: 传输方式
- `MCP_SCRATCHPAD_BASE_DIR`: 文件存储基础目录
- `MCP_SCRATCHPAD_MAX_FILE_SIZE`: 最大文件大小（字节）
- `MCP_SCRATCHPAD_LOG_LEVEL`: 日志级别（DEBUG/INFO/WARNING/ERROR/CRITICAL）
- `MCP_SCRATCHPAD_LOG_FORMAT`: 日志格式（text/json）
- `MCP_SCRATCHPAD_ALLOWED_NAMESPACES`: 允许的命名空间（逗号分隔）

### 配置示例

```bash
# 生产环境配置
export MCP_SCRATCHPAD_HOST=0.0.0.0
export MCP_SCRATCHPAD_PORT=8890
export MCP_SCRATCHPAD_LOG_LEVEL=INFO
export MCP_SCRATCHPAD_LOG_FORMAT=json
export MCP_SCRATCHPAD_MAX_FILE_SIZE=52428800  # 50MB

# 开发环境配置
export MCP_SCRATCHPAD_LOG_LEVEL=DEBUG
export MCP_SCRATCHPAD_LOG_FORMAT=text
```

## 部署建议

### Claude Desktop 配置

```json
{
  "mcpServers": {
    "scratchpad": {
      "command": "uv",
      "args": ["run", "--directory", "/path/to/mcp-scratchpad", "mcp-scratchpad"]
    }
  }
}
```

### Docker 部署

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install uv
RUN uv sync
EXPOSE 8890
CMD ["uv", "run", "mcp-scratchpad", "--transport", "sse", "--host", "0.0.0.0"]
